# Contracts

This folder contains typed interfaces that should guide implementation.

- `provider/provider-contract-v0.proto`: gRPC provider contract.
- `resources/resource-model-v0.yaml`: Fusion core resource model.

Treat these as versioned public contracts. Changes should be deliberate and documented with ADRs.

