# Roadmap Docs

V0 proves the kernel and adapter model. V1 expands toward the full 16-layer data engineering lifecycle.

The current recommendation is to build a strong MVP around the control plane kernel before attempting the full platform.

