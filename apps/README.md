# Apps

User-facing applications and frontend-facing API surfaces live here.

- `web/`: React/TypeScript Fusion operator UI.
- `api-gateway/`: Optional BFF/API gateway for frontend-facing REST or GraphQL APIs.

The UI should keep tenant and project context visible, show loading/error/empty states, and make AI-generated or dangerous changes approval-oriented.

