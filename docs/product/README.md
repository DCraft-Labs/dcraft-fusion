# Product Docs

The canonical definition is in `DCraft_Fusion_PROJECT_CONTEXT_FOR_CODEX_V2_EXPANDED.md`.

The short build brief is in `BUILD_UNDERSTANDING.md`.

Founder-level market validation and rollout planning is internal (not published in this docs tree).

The detailed Phase 1 build plan is in `PHASE_1_EXECUTION_PLAN.md`.

The detailed Phase 1 end-to-end implementation tracker is in `PHASE_1_END_TO_END_TRACKER.md`.

The latest Phase 1 completion summary is in `PHASE_1_COMPLETION_REPORT.md`.

The imported CDC engine evaluation is in `FUSION_CDC_ENGINE_EVALUATION.md`.

Short version: DCraft Fusion is an AI-assisted, engine-agnostic control plane for data platforms. It provides a governed UX and operating layer over existing systems instead of forcing customers into one engine.
